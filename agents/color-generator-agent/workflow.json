{
  "name": "Color Generator Agent",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "color-generator",
        "responseMode": "responseNode"
      },
      "id": "webhook-1",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "// Generate random RGB values\nconst r = Math.floor(Math.random() * 256);\nconst g = Math.floor(Math.random() * 256);\nconst b = Math.floor(Math.random() * 256);\n\n// Convert to hex\nconst toHex = (num) => num.toString(16).padStart(2, 0);\nconst hexCode = `#${toHex(r)}${toHex(g)}${toHex(b)}`;\n\nreturn [{\n  json: {\n    hex: hexCode,\n    rgb: {\n      r: r,\n      g: g,\n      b: b\n    },\n    rgb_string: `rgb(${r}, ${g}, ${b})`,\n    timestamp: new Date().toISOString()\n  }\n}];"
      },
      "id": "code-1",
      "name": "Generate Random Color",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        200,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "allEntries"
      },
      "id": "respond-1",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [
        400,
        0
      ]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Generate Random Color",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate Random Color": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
